 version: 3

 jobs:
  build:
    steps:
      - checkout
      - npm install
      - npm test
 workflows:
   version: 3
   build: # this is the name of our workflow
     jobs: # and here we list the jobs we are going to run.
       - build